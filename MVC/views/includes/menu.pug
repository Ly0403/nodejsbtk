.list-group
    if categories.length > 0
        each category in categories
            if category.id === selectedCategoryId 
                a.text-center.list-group-item.active(href="/category/"+category.id)  #{category.name}
            else  
                a.text-center.list-group-item(href="/category/"+category.id)  #{category.name}