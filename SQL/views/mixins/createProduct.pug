mixin createProduct(product)
    .card(style="width: 18rem;")
        img.card-img-top(src=`/img/${product.image}`, alt="...")
        .card-body
            h5.card-title #{product.name}
            p.card-text #{product.description}
            a.btn.btn-primary(href=`/products/${product.id}`) Details